<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Management System</title>
    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory Management System</title>
    <style>
        /* Reset some default styles */
        :root {
          --sage-green: #acc196;
          --amber-red: #ff6b6b;
          --dark-background: #fff;
        }

        body {
          font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
          background-color: var(--dark-background);
          color: #333;
          line-height: 1.6;
          margin: 0;
          padding: 0;
        }

        h1, h2, h3 {
          margin-bottom: 20px;
        }

        
        .container {
          max-width: 800px;
          margin: 0 auto;
          padding: 20px;
          background-color: #fff;
          box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        /* Responsive Design */
        @media screen and (max-width: 768px) {
          .container {
            max-width: 90%;
            padding: 15px;
          }
        }

                /* Navbar Styles */
                .navbar {
          background-color: var(--sage-green); /* Change background color */
          text-align: center;
          padding: 20px 0;
        }

        .navbar ul {
          list-style: none;
          display: flex;
          justify-content: center;
          padding: 0;
        }

        .navbar li {
          margin: 0 20px;
        }

        .navbar a {
          text-decoration: none;
          color: #fff; /* Change text color */
          font-weight: bold;
          transition: color 0.3s ease;
        }

        .navbar a:hover {
          color: var(--amber-red); /* Change text color on hover */
        }
        /* Style for visited links */
.navbar a:visited {
  color: #fff;
}
    </style>

</head>
<header>
    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/products">View Products</a></li>
            <li><a href="/add-product">Add Product</a></li>
            <!-- Add links for other features if needed -->
        </ul>
    </nav>
</header>
<body>
    <header>
        <h1>Welcome to the Inventory Management System</h1>
    </header>
    <div class="container">
  </body>
  <!-- home.ejs -->

</html>
